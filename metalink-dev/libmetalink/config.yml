name: libmetalink
versions:
  0.1.3:
    folder: all
template:
  from: 0.1.3
  folder: all
  url: https://github.com/metalink-dev/libmetalink/releases/download/release-${version}/libmetalink-${version}.tar.xz
