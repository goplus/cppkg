name: intx
versions:
  0.10.1:
    folder: all
  0.11.0:
    folder: all
  0.12.0:
    folder: all
template:
  from: 0.12.0
  folder: all
  url: https://github.com/chfast/intx/archive/v${version}.tar.gz
