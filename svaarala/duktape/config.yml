name: duktape
versions:
  2.5.0:
    folder: all
  2.6.0:
    folder: all
  2.7.0:
    folder: all
template:
  from: 2.7.0
  folder: all
  url: https://github.com/svaarala/duktape/releases/download/v${version}/duktape-${version}.tar.xz
