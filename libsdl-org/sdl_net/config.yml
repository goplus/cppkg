name: sdl_net
versions:
  2.0.1:
    folder: all
  2.2.0:
    folder: all
template:
  from: 2.2.0
  folder: all
  url: https://github.com/libsdl-org/SDL_net/releases/download/release-${version}/SDL2_net-${version}.tar.gz
