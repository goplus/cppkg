versions:
  0.4.0:
    folder: all
  0.5.0:
    folder: all
  0.5.1:
    folder: all
  0.5.2:
    folder: all
template:
  folder: all
  url: https://github.com/aklomp/base64/archive/v${version}.tar.gz
