versions:
  0.5.0:
    folder: all
  0.6.0:
    folder: all
  0.7.0:
    folder: all
  0.8.0:
    folder: all
template:
  folder: all
  url: https://github.com/lemire/fast_double_parser/archive/v${version}.tar.gz
