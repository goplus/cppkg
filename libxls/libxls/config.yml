name: libxls
versions:
  1.6.2:
    folder: all
  1.6.3:
    folder: all
template:
  from: 1.6.3
  folder: all
  url: https://github.com/libxls/libxls/releases/download/v${version}/libxls-${version}.tar.gz
