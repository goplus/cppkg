name: cajun-jsonapi
versions:
  2.0.3:
    folder: all
  2.1.1:
    folder: all
template:
  from: 2.1.1
  folder: all
  url: https://github.com/cajun-jsonapi/cajun-jsonapi/archive/refs/tags/${version}.tar.gz
