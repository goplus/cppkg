name: xproperty
versions:
  0.11.0:
    folder: all
  0.12.0:
    folder: all
template:
  from: 0.12.0
  folder: all
  url: https://github.com/jupyter-xeus/xproperty/archive/refs/tags/${version}.tar.gz
