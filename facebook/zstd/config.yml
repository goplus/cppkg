versions:
  1.5.0:
    folder: all
  1.5.2:
    folder: all
  1.5.5:
    folder: all
  1.5.6:
    folder: all
  1.5.7:
    folder: all
template:
  folder: all
  url: https://github.com/facebook/zstd/releases/download/v${version}/zstd-${version}.tar.gz
