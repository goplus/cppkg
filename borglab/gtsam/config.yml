name: gtsam
versions:
  4.0.3:
    folder: all
  4.1.1:
    folder: all
  "4.2":
    folder: all
template:
  from: "4.2"
  folder: all
  url: https://github.com/borglab/gtsam/archive/refs/tags/${version}.tar.gz
