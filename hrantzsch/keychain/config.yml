name: keychain
versions:
  1.2.0:
    folder: all
  1.2.1:
    folder: all
  1.3.0:
    folder: all
template:
  from: 1.3.0
  folder: all
  url: https://github.com/hrantzsch/keychain/archive/refs/tags/v${version}.tar.gz
