cmake_minimum_required(VERSION 3.15)
project(test_package LANGUAGES C)

find_package(msgpack REQUIRED CONFIG)

add_executable(${PROJECT_NAME} test_package.c)
if(TARGET msgpackc)
    target_link_libraries(${PROJECT_NAME} PRIVATE msgpackc)
else()
    target_link_libraries(${PROJECT_NAME} PRIVATE msgpack-c)
endif()
gpack-cxx_VERSION VERSION_GREATER_EQUAL "6.0.0")
    target_link_libraries(${PROJECT_NAME} msgpack-cxx)
else()
    target_link_libraries(${PROJECT_NAME} msgpackc-cxx)
endif()
target_compile_features(${PROJECT_NAME} PRIVATE cxx_std_11)
