name: libexif
versions:
  0.6.23:
    folder: all
  0.6.24:
    folder: all
template:
  from: 0.6.24
  folder: all
  url: https://github.com/libexif/libexif/releases/download/v${version}/libexif-${version}.tar.bz2
