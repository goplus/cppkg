versions:
  1.0.23:
    folder: all
  1.0.24:
    folder: all
  1.0.25:
    folder: all
  1.0.26:
    folder: all
template:
  folder: all
  url: https://github.com/libusb/libusb/releases/download/v${version}/libusb-${version}.tar.bz2
