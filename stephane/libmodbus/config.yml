name: libmodbus
versions:
  3.1.10:
    folder: all
  3.1.6:
    folder: all
  3.1.8:
    folder: all
template:
  from: 3.1.10
  folder: all
  url: https://github.com/stephane/libmodbus/releases/download/v${version}/libmodbus-${version}.tar.gz
