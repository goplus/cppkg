versions:
  1.5.1:
    folder: all
  1.6.0:
    folder: all
  1.7.0:
    folder: all
  1.7.1:
    folder: all
template:
  folder: all
  url: https://github.com/OSGeo/libgeotiff/releases/download/${version}/libgeotiff-${version}.tar.gz
