name: sentry-native
versions:
  0.6.6:
    folder: all
  0.7.20:
    folder: all
  0.8.1:
    folder: all
  0.8.3:
    folder: all
template:
  from: 0.8.3
  folder: all
  url: https://github.com/getsentry/sentry-native/releases/download/${version}/sentry-native.zip
